# CLAUDE.md

このファイルは、このリポジトリで作業する未来の自分（クラウド上で起動する Claude）への申し送りです。
**起動したら、新しいことに着手する前に必ずこのファイルを読むこと。**

---

## 🛑 最優先の習慣：着手前に「ノートくん」に聞く

あなた（クラウド側の自分）は、セッションごとに記憶がリセットされて起動します。
そのため「過去に同じことを調べた／同じ落とし穴にハマった」という事実を、**自分が忘れていることすら忘れたまま**自己流で突っ走りがちです。

これを防ぐための鉄則：

> **新しいタスク・初めてのトピック・「前どうやったっけ？」と思った瞬間は、自分で動き出す前に、まずノートくん（長期記憶）に聞く。例外なし。**

特に次のキーワードが出たら危険ゾーン。動く前に一度止まってノートくんに確認する：

- 「〜見に行って」「〜チェックして」「〜やって」（具体的なタスク指示）
- 「前にどうやったっけ？」「過去の解決策は？」（過去の経緯を問う）
- 「迷ったら」「初めて」「新しい」（方針が未確定）

ノートくんには「**前にどうやったか / 落とし穴・注意点は何か / 既存のスクリプトや成果物はあるか**」を聞く。
記憶が曖昧なまま作業を続けないこと。

---

## 📮 ノートくんに聞く方法（Drive ブリッジ）

ノートくん（長期記憶）は別マシン上にあり、**Google Drive 上の受け渡しフォルダ越し**に質問できます。
クラウド側の自分が直接ノートくんを叩くことはできませんが、このブリッジ経由なら、相手の端末が起きてさえいれば記憶に届きます。

### 仕組み

1. **質問を置く** … `notebook_inbox` という名前の Drive フォルダに、質問を JSON ファイルとして作成する。
2. **待つ** … 相手側で動いている定期ポーラーが、数分以内（最大5分目安）に質問を拾い、ノートくんに聞いて、回答を `notebook_outbox` フォルダに置く。
3. **回答を読む** … `notebook_outbox` から回答 JSON を読む。

> フォルダの ID はここには書かない（このリポジトリは公開）。
> 必要なときは Drive 検索でフォルダ名から見つけること：`title = 'notebook_inbox'` / `title = 'notebook_outbox'`。

### 質問 JSON の形

`notebook_inbox` に、以下の形の JSON ファイルを作成する
（テキスト内容として保存。Google 形式への自動変換は無効にする）。

```json
{
  "version": "1.0",
  "id": "q_好きな一意のID",
  "from": "cloud-kuro",
  "timestamp": "ISO8601のタイムスタンプ",
  "target": "dashboard",
  "question": "聞きたいこと",
  "status": "pending"
}
```

#### `target` の選択肢（聞きたい内容で使い分ける）

| target | 用途 |
|---|---|
| `dashboard` | 全般・既定。迷ったらこれ |
| `ai_library` | AI のトレンド・動向まわり |
| `ai_refs` | 公式仕様・リファレンス系 |

### 回答の読み方

`notebook_outbox` に、`answer_<質問ID>_<時刻>.json` という名前で回答が現れる。
読むと、主に次のフィールドが入っている：

- `answer` … 回答本文
- `sources_used` … 出典（ノートくんが根拠にした記憶のID）
- `ok` … 成否（`true` なら成功）

回答待ちの間は、こちら側で時間を進めて待ち続けることはできない。
質問を置いたら一度ユーザーに「数分後に確認する」と伝え、頃合いを見て `notebook_outbox` を読みに行くこと。

---

## まとめ

- 記憶はリセットされる。だから**自己流で動く前に、まずノートくんに聞く**。
- 聞く手段はこのリポジトリに書いてある（上記ブリッジ）。「知らなかった」はもう言い訳にならない。
- ユーザーがいちいち「ノートくんに聞いて」と言わなくても、自分から聞くこと。
